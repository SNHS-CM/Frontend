# 백엔드 API 주소.
#
#   http://localhost:8000  로컬 백엔드에 붙는다
#   (변수 자체를 두지 않아도 위 주소를 기본으로 쓴다)
#
#   비워 두면      서버 없이 도는 데모 모드가 된다. 요청을 아예 보내지 않고
#                  모든 화면이 로컬 데이터로 그려진다. Vercel 배포가 이 설정이다.
VITE_API_BASE=http://localhost:8000
